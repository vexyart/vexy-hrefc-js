1. I want the popups to appear faster. The defaulut should be that once the library is initialized and all other code is executed, the library should prefetch the metadata from the linked pages. Only if the user requests lazy resolution, this should not happen. 

2. I want to be able to specify additional classes through which I would control certain behaviors. In other words, there might be a default class that might be `hrefc` that triggers the popups by itself, and also there should be a way to just trigger popups on ALL `a` links. But then I should be able to specify several configs, like `<a href='...' class='hrefc'>` only fetches title and description but `<a href='...' class='hrefc thumb'>` also fetches thumbnail. Alternatively this might also be configured via `data-` attributes.


